datastore:
  log_dbms:
    database_uri: mysql+mysqlconnector://user:password@host/dbname

  statistic_dbms:
    type: mongodb
    host: mongodb host
    port: mongodb port
    database: mongodb database name

processor:
  system_schema:
    # dir for system schemas
    dir: ./system_shcema

  chart_schema:
    # file path of chart schema
    path: ./chart_data.schema.sample.yaml